# E-Commerce Backend

FastAPI backend for the e-commerce monolith learning project.

## Setup

```bash
poetry install
poetry run python -m app.main
```

## Development

```bash
poetry run uvicorn app.main:app --reload
```
